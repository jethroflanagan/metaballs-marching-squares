const margin = 10;

export const dimension = {
    margin,
    width: 350 - margin * 2,
    height: 600 - margin * 2,
}
export const grid = {
    cellWidth: 20,
    cellHeight: 20,
}
export const balls = {
    numToAdd: 4,
}
