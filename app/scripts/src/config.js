const margin = 10;

export const dimension = {
    margin,
    width: 350 - margin * 2,
    height: 600 - margin * 2,
}
export const grid = {
    cellWidth: 8,
    cellHeight: 8,
}
export const balls = {
    numToAdd: 6,
}
